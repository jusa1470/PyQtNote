#!/usr/bin/env python

from distutils.core import setup

setup(name='Task Note',
      version='0.0.1',
      description='Notes for your tasks',
      py_modules=['PyQtNote']
      # py_modules = ['main', 'pages', 'qtPackage']
     )